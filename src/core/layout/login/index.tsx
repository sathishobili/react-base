import React from "react";
import { Outlet } from "react-router-dom";

// ==============================|| MINIMAL LAYOUT ||============================== //

const LoginLayout = () => (
  <>
    <Outlet />
  </>
);

export default LoginLayout;
